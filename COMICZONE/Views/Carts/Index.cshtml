@using COMICZONE.Extensions;

@model COMICZONE.Models.Cart

@{
    ViewData["Title"] = "Giỏ hàng";
}

<div class="container mt-4">

    <h2 class="mb-4">
        <i class="bi bi-cart"></i> Giỏ hàng của bạn
    </h2>

    @if (Model == null || Model.CartItems == null || !Model.CartItems.Any())
    {
        <div class="text-center p-5 border rounded bg-light">
            <h4>🛒 Giỏ hàng của bạn đang trống</h4>
            <p class="text-muted">Hãy thêm sản phẩm để tiếp tục mua sắm</p>

            <a asp-controller="Home"
               asp-action="Index"
               class="btn btn-primary mt-3">
                Tiếp tục mua sắm
            </a>
        </div>
    }
    else
    {
        <div class="row mt-4">

            <!-- LEFT: CART TABLE -->
            <div class="col-lg-8">

                <div class="card shadow-sm">
                    <div class="card-body">

                        <div style="max-height:400px; overflow-y:auto;">
                            <table class="table align-middle">

                                <thead class="table-light sticky-header">
                                    <tr>
                                        <th>Product</th>
                                        <th width="120">Price</th>
                                        <th width="120">Quantity</th>
                                        <th width="120">Total</th>
                                        <th width="100"></th>
                                    </tr>
                                </thead>

                                <tbody>
                                    @foreach (var item in Model.CartItems)
                                    {
                                        <tr>

                                            <td>
                                                <div class="d-flex align-items-center">

                                                    <img src="@item.Product?.GetImagePath()"
                                                         width="60"
                                                         height="60"
                                                         class="rounded me-3"
                                                         style="object-fit:cover;" />

                                                    <strong>@item.Product?.Name</strong>

                                                </div>
                                            </td>

                                            <td>
                                                @((item.Product?.Price ?? 0).ToString("N0")) ₫
                                            </td>

                                            <td>
                                                <input type="number"
                                                       class="form-control text-center quantity-input"
                                                       value="@item.Quantity"
                                                       min="1"
                                                       data-cartitem-id="@item.CartItemId"
                                                       style="width:60px">
                                            </td>

                                            <td>
                                                <strong class="text-danger item-total">
                                                    @((item.Quantity * (item.Product?.Price ?? 0)).ToString("N0")) ₫
                                                </strong>
                                            </td>

                                            <td>
                                                <a asp-action="Remove"
                                                   asp-route-cartItemId="@item.CartItemId"
                                                   class="btn btn-outline-danger btn-sm">
                                                    <i class="bi bi-trash"></i>
                                                </a>
                                            </td>

                                        </tr>
                                    }

                                </tbody>

                            </table>
                        </div>

                    </div>
                </div>

            </div>

            <!-- RIGHT: CHECKOUT CARD -->
            <partial name="_Payment" model="Model" />

        </div>
    }

</div>
